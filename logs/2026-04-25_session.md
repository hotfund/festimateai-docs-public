# 세션 로그 — 2026-04-25 (제주 longlist 추가 + 비번 재설정)

> **세션 성격**: 2대 PC 병행 — 이 PC Desktop(지휘) + 이전 PC 파워셀(실행)
> **시작 커밋**: `5b33cd3` (4/24 저녁 stage2_promote 지역별 publish_status 분리)
> **현재 커밋**: `64f9f0c` (제주 city prefix + target city 추가)
> **다음 세션 이어받기**: 이 파일 읽고 §4 "현재 중단 지점"부터 재개

---

## 1. 세션 구조 ⭐ 중요

**2대 PC 병행 작업 패턴** (항상 이 구조로 함):

| PC | 사용자 | 역할 |
|----|-------|------|
| **이 PC** (USER) | 대표님 | Claude Desktop = 저(Claude)와 대화·지휘·설계 |
| **이전 PC** (iksong) | 대표님 병행 사용 | 파워셀 = 실제 실행 (batch, ETL, DB 쓰기) |

- 제가 파워셀 명령을 코드박스로 제공
- 대표님이 이전 PC 파워셀에 복붙·실행
- 결과를 저에게 보고
- 대화는 **항상 이 PC의 Desktop에서** 진행

---

## 2. 오늘 이어받은 맥락 (어제 4/24 이전 PC 작업 15 커밋)

어제 밤 이전 PC에서 작업한 것들이 오늘 이 PC `git pull`로 들어옴:

```
5b33cd3  feat(etl): stage2_promote — 지역별 publish_status 분리
7546326  feat(db): publish_status 분리 + stage2_promote review 기준 완화
fee7410  feat(db): 언어지원 정책 v1.0 — language_tier + 5개 필드 마이그레이션
e4ef06b  feat(etl): homepage_discovery — Google CSE → Naver Search API 교체
ad1a722  fix(etl): stage2_promote --dry-run 실제 롤백 지원 추가
3a7a373  feat(db): migration 017 — admin_users 테이블 + is_admin() 헬퍼
194d0c4  fix(etl): seed_load dedup + 경기도 target city 추가
c345c23  chore: add Dockerfile for Railway ETL worker
cd30ee5/aa8e531  fix: nixpacks 이슈
73fa9f8  feat(ai-planner): Phase C-1 — tripDays + entrySource + sessionId
3f5358a  docs: CLAUDE.md — 3자 병렬 검토 워크플로우
3ca6e19  docs(logs): 2026-04-22 세션 압축 복원
11bcfba  docs: INFRA_STATUS v2.0
```

**핵심 함의**:
- DB migration 017/018/019 신규 (admin_users, language_tier, publish_status)
- stage2_promote에 `CORE_CITIES`로 제주 포함
- homepage batch Naver API 사용 (Google CSE 교체)
- Railway Dockerfile 도입 (nixpacks 대체)

---

## 3. 오늘 완료한 작업

### 3-1. 환경 정리
- 이 PC에 git pull로 15 커밋 이어받음
- 이 PC `npm install` + `npx vitest run` → **9 files, 95 tests pass** (기준선 깨끗)
- `npx tsc --noEmit` → 0 errors

### 3-2. 제주 누락 발견
`scripts/etl/_common.py`:
- `_TARGET_CITIES`에 "제주" 누락
- `_CITY_PREFIX_MAP`에 제주 prefix 없음

반면 `scripts/etl/stage2_promote.py`:
- `CORE_CITIES = ('서울','부산','경기','제주','인천','대구')` — 제주가 "핵심 관광권"

**불일치**: 제주가 `pending_publish` 자동 승급 대상이지만 **longlist 진입 경로 자체가 차단**된 상태. `SELECT COUNT(*) FROM clinic_longlist_seed WHERE full_address_raw LIKE '%제주%'` → **0**.

### 3-3. DB 비번 재설정 (Supabase)
- 이전 비번 `festimate2026!` 여러 곳에 노출 → 재설정 필요
- **대화 중 비번 3차 연쇄 노출 사고** 발생 (대괄호 placeholder 오해 포함)
- 최종: Supabase에서 URL-safe 비번 재설정 완료
- 비밀번호 관리자에만 저장, 대화창에는 안 남김
- 이 PC의 `$env:DATABASE_URL` 새 비번으로 세팅 → 연결 확인 (seed count: 90,410)

### 3-4. 제주 추가 (`_common.py` 수정)
- `_CITY_PREFIX_MAP`에 제주특별자치도/제주도/제주시/제주 prefix 4개 추가
- `_TARGET_CITIES`에 "제주" 추가
- 검증: `is_target_city('제주')` → True, `parse_city_district('제주특별자치도 제주시 노형동')` → `('제주', '제주시', '노형동')`
- 커밋 `64f9f0c` 푸시

### 3-5. 도시별 분포 확인 (현재 90,410개 중)

| 도시 | 개수 |
|------|------|
| 서울 | 30,622 |
| 경기 | 24,053 |
| None (파싱 실패) | 9,348 |
| 부산 | 8,513 |
| 대구 | 5,500 |
| 인천 | 5,096 |
| 광주 | 3,926 |
| 대전 | 3,352 |
| **제주** | **0** |

→ 기존 seed_load는 **target 8개 도시만 로드**된 상태. 제주는 파일 자체에 없었거나 도시별 분할 CSV 방식.

### 3-6. 이전 PC 준비
- 이전 PC에서 `cd C:\FestimateNew`
- 이전 PC에서 `git pull` → `64f9f0c` 반영 완료
- 이전 PC에서 `$pw = Read-Host`로 새 비번 입력 완료

---

## 4. 현재 중단 지점 ⭐ 다음 세션 시작점

### Step 22-D — 이전 PC에서 실행 대기 중
```powershell
$env:DATABASE_URL = "postgresql://postgres.cvqxzxymtvtfzfgamawy:$pw@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres"
```

### 다음 단계 (Step 23~26)

**Step 23**: DB 연결 테스트
```powershell
python -c "import os, psycopg2; conn = psycopg2.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM festimate_core.clinic_longlist_seed'); print('seed count:', cur.fetchone()[0]); conn.close()"
```
기대: `seed count: 90410`

**Step 24**: 제주 CSV 파일명 확인
```powershell
ls data/mois/
```
(제주 CSV가 `C:\FestimateNew\data\mois\` 안에 있음 — 어제 받다가 중단)

**Step 25**: 파일이 완전한지 확인
```powershell
Get-ChildItem data/mois -Filter "*제주*" | Select-Object Name, Length, LastWriteTime
```

**Step 26**: seed_load 실행
```powershell
python -m scripts.etl.seed_load --file data/mois/<제주CSV파일명>.csv
```

**Step 27**: 결과 확인
- 제주 clinic이 longlist에 들어갔는지
- `is_target_region=TRUE`인지

---

## 5. 글로벌 룰 (이 세션에서 확정됨)

대표님과 일하는 방식:

| # | 룰 |
|---|---|
| 1 | 파일 기록 전 **승인 + 예상시간** 명시 |
| 2 | URL은 항상 **클릭 가능한 하이퍼링크** 형식 (`[텍스트](URL)`) |
| 3 | **파워셀 명령은 한 번에 하나씩** (세미콜론 체이닝 X) |

---

## 6. 역할 분담 재정의

CLAUDE.md 공식 분담 vs 실제 운영:

| | 공식 (CLAUDE.md) | 실제 |
|---|------|------|
| **Desktop** (나) | 기획 전담 | 지휘·설계 + 소통 창구 + 파워셀 명령 작성 |
| **Claude Code CLI** (실무 별도) | 코딩 전담 | **거의 안 쓰는 중** — 파워셀 CLI가 이 역할 대체 |
| **파워셀** (이전 PC) | 공식 분담에 없음 | **실제 코딩·실행 담당** (Python, git, psql) |
| **Genspark** | 조언자 | 조언자 유지 |

- "파워셀 = CLI"로 대표님이 재정의
- 저(Desktop)의 핵심 역할은 **소통 창구 + 지휘 센터**
- 대표님이 제 지시대로 파워셀에 명령 입력

---

## 7. 보안 주의 — 비번 유출 히스토리

**이 세션에서 3번 노출됐음**:
1. 첫 번째 비번 `festimate2026!` (이전 세션에서)
2. 두 번째 재설정 `snit5858@@TT` (이 세션 시작 시)
3. 세 번째 재설정 `Fse45NYu7Yz6Q0b0` (대괄호 실수)

**최종 비번**: 위 3개 다 폐기됨. 4번째 재설정한 것만 유효 (비밀번호 관리자에만 저장).

**사고 원인**:
- 제가 `[여기에_비번]` 같은 대괄호 placeholder 사용 → 대표님이 대괄호 포함해서 치환 실수
- 대표님이 이전 Claude 답변에서 받은 비번 포함 메시지를 저에게 복붙 → 노출

**개선 사항**:
- 이 세션부터 `Read-Host` 방식만 사용 (파워셀이 직접 비번 받음)
- 대화창에 비번 포함된 메시지 붙이지 않기
- placeholder 대신 Read-Host 변수 사용

**권장**: Naver API 토큰(`0meJ91QNQ4ojaom6TQ5a` / `o4jKptIvTn`)도 노출됐으므로 [Naver Developers](https://developers.naver.com/apps/)에서 재발급 권장 (지금은 아니고 여유 있을 때).

---

## 8. 주요 URL

- [Supabase hotfund/festimateNew → Database](https://supabase.com/dashboard/project/cvqxzxymtvtfzfgamawy/settings/database)
- [Supabase Connect](https://supabase.com/dashboard/project/cvqxzxymtvtfzfgamawy/database/schemas?showConnect=true)
- [Railway Dashboard](https://railway.app/dashboard)
- [Naver Developers](https://developers.naver.com/apps/)
- [data.go.kr 행정안전부_건강_의원](https://www.data.go.kr/data/15088235/fileData.do)

---

## 9. 남은 리스크 / 미결

| 항목 | 상태 |
|------|------|
| Railway DATABASE_URL 업데이트 | ⚠️ 미완 — Railway → festimateNew → Variables에서 새 비번으로 업데이트 필요 |
| Naver API 토큰 재발급 | ⚠️ 노출됨 — 여유 있을 때 재발급 |
| Migration 015 RLS 활성화 | ⏳ 어제 4/22 로그의 미결 — admin_users 만들어졌으니 다음에 활성화 가능 |
| AI Planner Phase C | ⏳ C-1 완료 (`73fa9f8`), C1(DTO)~C4(Auth) 대기 |
| 제주 CSV 로드 | 🔄 현재 진행 중 (Step 22-D) |

---

## 10. 커밋 히스토리 (오늘)

```
64f9f0c  fix(etl): _common.py 제주 city prefix + target city 추가
```

---

*작성: 2026-04-25 세션 중간 (진행 중 스냅샷)*

---

## 11. 🚀 이어받기 (이전 PC Claude Desktop에서)

**상황**: 대표님이 이 PC(Desktop)와 이전 PC(파워셀) 사이 카톡 경유가 너무 번거로워서, **이전 PC의 Claude Desktop `festimatenew` 프로젝트에서 직접 이어서 작업**하기로 결정.

### 이전 PC Claude Desktop에서 첫 메시지 (복사 붙여넣기)

```
logs/2026-04-25_session.md §11 읽고 이어서 작업 시작.

현재 상태:
- 이전 PC 파워셀에서 $pw = Read-Host로 비번 입력 완료
- $env:DATABASE_URL 조립 완료 (Step 22-D)
- 다음: Step 23 DB 연결 테스트부터

대표님 원칙 (글로벌 룰):
1. 파일 기록 전 승인 + 예상시간 명시
2. URL은 항상 하이퍼링크 형식
3. 파워셀 명령 한 번에 하나씩 (체이닝 금지)

역할: 너=Claude Desktop(지휘), 이 PC 파워셀=실행. 
파워셀 명령을 내가 이 PC 파워셀에 직접 붙여넣고 결과를 너에게 보고.
비번 대화창에 노출 금지.

Step 23부터 한 줄씩 명령 줘라.
```

---

### ✅ 완료된 단계 (건너뛰기)

- [x] Step 1~21: 이 PC에서 환경 점검 + `_common.py` 제주 추가 커밋 (`64f9f0c`)
- [x] Step 22-A: 이전 PC `cd C:\FestimateNew`
- [x] Step 22-B: 이전 PC `git pull` → `64f9f0c` 반영
- [x] Step 22-C: 이전 PC `$pw = Read-Host "DB password"` 비번 입력
- [x] Step 22-D: 이전 PC `$env:DATABASE_URL = "..."` 조립

---

### ⏳ 남은 단계 (Step 23~28) — 파워셀 명령

#### Step 23: DB 연결 테스트

```powershell
python -c "import os, psycopg2; conn = psycopg2.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM festimate_core.clinic_longlist_seed'); print('seed count:', cur.fetchone()[0]); conn.close()"
```

**기대**: `seed count: 90410`

---

#### Step 24: `data/mois/` 전체 파일 목록

```powershell
Get-ChildItem data/mois | Sort-Object LastWriteTime -Descending | Select-Object Name, Length, LastWriteTime | Format-Table -AutoSize
```

**기대**: 최근 수정 순서로 CSV 파일 리스트

---

#### Step 25: 제주 관련 파일 필터

```powershell
Get-ChildItem data/mois -Filter "*제주*" | Select-Object Name, Length, LastWriteTime
```

또는 영문명 시도:

```powershell
Get-ChildItem data/mois -Filter "*jeju*" | Select-Object Name, Length, LastWriteTime
```

---

#### Step 26: `seed_load` 실행

파일명 확정 후 (예시 `제주_의원_20260424.csv`):

```powershell
python -m scripts.etl.seed_load --file data/mois/<실제파일명>.csv
```

**기대**: 진행 로그 + `batch 완료: N rows processed`

---

#### Step 27: 제주 결과 검증

```powershell
python -c "import os, psycopg2; conn = psycopg2.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM festimate_core.clinic_longlist_seed WHERE city = %s', ('제주',)); print('jeju count:', cur.fetchone()[0]); conn.close()"
```

**기대**: `jeju count: <양수>` (0이 아닌 수)

---

#### Step 28: 전체 도시 분포 재확인

```powershell
python -c "import os, psycopg2; conn = psycopg2.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute('SELECT city, COUNT(*) FROM festimate_core.clinic_longlist_seed GROUP BY city ORDER BY 2 DESC LIMIT 15'); rows = cur.fetchall(); [print(r) for r in rows]; conn.close()"
```

**기대**: 기존 8개 도시 + 제주 추가된 목록

---

### 🛑 제주 로드 끝난 후 마무리

1. **세션 로그 §11-결과 추가 커밋** — Claude Desktop이 정리
2. **Railway Variables 업데이트** — 브라우저 [Railway Dashboard](https://railway.app/dashboard) → festimateNew → Variables → DATABASE_URL
3. **이 PC 복귀** — 이전 PC는 임시 파일 저장소 역할 완료, 꺼놔도 됨

---

### 📝 참고 정보

**현재 Supabase DB**:
- Project: `hotfund/festimateNew` (ID: `cvqxzxymtvtfzfgamawy`)
- Tokyo region
- Pooler: `aws-1-ap-northeast-1.pooler.supabase.com:6543`

**최신 커밋**:
```
1779752  docs(logs): 2026-04-25 세션 중간 스냅샷
64f9f0c  fix(etl): _common.py 제주 city prefix + target city 추가
```

**현재 longlist seed 총 90,410개** (제주 0) — Step 27~28로 증가 확인.

---

*업데이트: 2026-04-25 이전 PC Desktop 이어받기 지시*

---

## 12. ✅ 세션 완료 — 제주 작업 결과

### 최종 확인 결과

| 항목 | 값 |
|------|-----|
| 전체 seed count | **91,766** (이전 90,410에서 +1,356) |
| 제주 seed count | **1,312** |
| 제주 is_target_region=TRUE | **1,312** (전부 TRUE) |
| 제주 is_target_region=FALSE | 0 |

### 최종 도시별 분포

| 도시 | 개수 |
|------|------|
| 서울 | 30,622 |
| 경기 | 24,053 |
| None (파싱 실패) | 9,392 (+44 from 9,348) |
| 부산 | 8,513 |
| 대구 | 5,500 |
| 인천 | 5,096 |
| 광주 | 3,926 |
| 대전 | 3,352 |
| **제주** | **1,312** ✅ |
| **합계** | **91,766** |

### 의미

- 제주 clinic이 **`_common.py` 제주 추가 이후** 로드됨 (is_target_region=TRUE)
- 이제 `stage2_promote`가 돌 때 제주 clinic이 자동으로 **`pending_publish`** 로 승급 가능 (CORE_CITIES에 포함됨)
- 재개된 homepage batch가 **제주 clinic 홈페이지도 자동 수집** 가능

### seed_load는 언제 실행됐나
- 대표님이 homepage batch 재개할 때 같이 또는 그 전후에 실행한 것으로 추정
- 우리가 이 세션에서 명시적으로 실행하진 않았음
- 결과적으로 작업 완료

---

## 13. 🔬 근본 해결책 (다음 세션 논의)

### 오늘 체감한 문제
- CSV 원본 파일이 **로컬 PC에만 있음** → 두 PC 왔다갔다 번거로움
- DB와 코드는 이미 클라우드에 공유돼 있지만, CSV만 로컬 분산

### 제안 3단계

#### 🔴 단기 (오늘~이번 주)
- 이번 세션에서 발생한 혼란 줄이기
- 제주 CSV는 이미 DB에 들어갔으니 로컬 파일 의존 끝

#### 🟡 중기 (이번 주 내)
**Supabase Storage 도입**
- Supabase는 파일 스토리지도 제공 (S3 호환)
- `data/mois/` 원본 CSV를 Supabase Storage `raw-csv` 버킷에 업로드
- `seed_load.py` 수정 — 파일 경로 대신 Storage URL 받기
- 어느 PC에서든 다운로드·실행 가능
- DB + 원본 파일 **모두 같은 클라우드**

#### 🟢 장기 (다음 달)
**Railway Worker + 자동화**
- data.go.kr 새 버전 감지 → Supabase Storage에 자동 업로드 → Railway에서 seed_load 자동 실행
- 로컬 PC 역할 제거
- 완전한 프로덕션 구조

---

## 14. 📋 오늘 커밋 히스토리

```
06fad28  docs(logs): 2026-04-25 §11 이어받기 섹션 추가
1779752  docs(logs): 2026-04-25 세션 중간 스냅샷
64f9f0c  fix(etl): _common.py 제주 city prefix + target city 추가
```

미러 동기화:
```
920f430  sync: 2026-04-25 §11 handoff section
f12ff28  sync: 2026-04-25 session log (mid-session snapshot)
```

---

## 15. 🚧 미결 사항 (다음 세션에)

| 항목 | 우선순위 | 메모 |
|------|---------|------|
| **Railway Variables 업데이트** | 🔴 P0 | 새 DB 비번으로 업데이트 안 하면 Railway Worker 인증 실패 |
| **Naver API 토큰 재발급** | 🟡 P1 | 이 대화에 노출됨. 여유 있을 때 |
| **Supabase Storage 구조 도입** | 🟡 P1 | 양 PC 오가는 문제 근본 해결 |
| **Migration 015 RLS 활성화** | 🟡 P1 | admin_users 만들어졌으니 가능 |
| **AI Planner Phase C 계속** | 🟢 P2 | C1(DTO) → C2(결과 화면) → C3(Matching) → C4(Auth) |

---

## 16. 🎯 다음 세션 시작 메시지 (제안)

```
logs/2026-04-25_session.md 읽고 시작하자.

현재 상태:
- 제주 작업 완료 (1,312개, is_target_region=TRUE)
- main HEAD: (이번 세션 마지막 커밋 해시)

오늘 하고 싶은 작업:
1. Railway Variables 업데이트 (P0)
2. 또는 Supabase Storage 구조 도입 (P1, 근본 해결)
3. 또는 AI Planner Phase C 계속

어느 것부터?
```

---

## 17. 🙏 오늘 세션 감사

- 제주 누락 발견부터 해결까지 완료
- DB 비번 재설정 (4차 최종)
- 2대 PC 협업 구조 이해
- 글로벌 룰 3개 확정 (승인·URL·한 줄씩)
- 앞으로 재발 방지 방향 논의

**대표님 정말 수고 많으셨습니다.**

---

*최종 작성: 2026-04-25 세션 완료*
